version: "3.8"

services:
  mc:
    image: itzg/minecraft-server
    tty: true
    stdin_open: true
    ports:
      - "25565:25565"
    environment:
      TYPE: "FABRIC"
      PACKWIZ_URL: "https://ryleu.me/cch-mc-server/packwiz/pack.toml"
      EULA: "TRUE"
      MOTD: "CCH Server"
      MEMORY: "4G"
      DIFFICULTY: "HARD"
      SPAWN_PROTECTION: "0"
      VIEW_DISTANCE: "8"
      OPS_FILE: "https://ryleu.me/cch-mc-server/ops.json"
      EXISTING_WHITELIST_FILE: "SYNCHRONIZE"
      WHITELIST_FILE: "https://ryleu.me/cch-mc-server/whitelist.json"
    volumes:
      # attach the relative directory 'data' to the container's /data path
      - ./data:/data
